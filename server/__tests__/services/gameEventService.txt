import { GameEventModel } from "../../src/models/GameEvent";
import { EventType, IGameEvent } from "../../src/types/types";
import mongoose from "mongoose";

const gameEvent: Array<Partial<IGameEvent>> = [
    {
        type: EventType.BUDGET_CUT,
        severity: 10,
        effectOnMorale: 10,
    },
];

describe("GameService", () => {
let mockSave: jest.Mock;

beforeEach(() => {
    mockSave = jest.fn();
    jest.spyOn(GameEventModel.prototype, "save").mockImplementation(mockSave);
  });

  afterEach(() => {
    jest.restoreAllMocks();
  });

  test.todo("Should get all gameser")
})
